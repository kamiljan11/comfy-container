# Changelog

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), wersjonowanie: [SemVer](https://semver.org/).
Kazdy PR dopisuje zmiany do [Unreleased]; przy release przenosimy pod numer wersji z data.

## [Unreleased]

- The site assistant now knows the site it sits on. Its prompt is a closed world, and it had never been told about the six service pages, the six area pages, the 20 case studies, the free 30-minute consultation that the contact form books, CetusPro joining for bigger builds, or Workshop 3.0. It also claimed Ekomoc was a private build with no URL, which is wrong twice over: it runs in production and its write-up is public. The assistant now answers a named problem by pointing at the one page that covers it, and it never sends anyone to the blog, which has no articles yet. `src/server/bot.prompt.test.ts` fails if a service or area page is added or renamed and the prompt is not updated with it.

- Workshop 3.0 in Selected projects now opens its GitHub write-up (`kamiljan11/mas-warsztat`) like every other row, instead of the login screen of the live app. The write-up says what the system does, how tenants are kept apart and what it runs on; the application itself stays private. The live address is in the write-up.
- Hero subtitle cut from 295 characters to 129 (Kamil: "mega za długo jak na hero"). The headline already promises less manual work, so the subtitle no longer restates it in three sentences: it names the work that leaves the team (quotes, retyping data between programs, customer replies) and keeps the line about training the team afterwards.

- The chat panel's own WhatsApp and e-mail links are 44 px tap targets on phones, as are the links in the lead form's error message; they measured 60x30, the only chat controls genuinely under 44. Checking the live site after the mobile pass also settled a false alarm worth writing down: an audit that measures the chat while it is closed reads every control inside it as 43 px, because the closed panel renders at `scale(.97)`. Open, they are 44. A new phone test opens the panel before measuring. Live numbers after the pass, 22 routes at 360/390/430 px: no sideways scroll, no text under 12 px (was 327), no form field under 16 px.

### Added

- Command palette (Kamil: "dodaj w menu też paletę komend"): a search button in the header and Ctrl+K / ⌘K from any page open a dialog that lists every page, the six services, the six areas and all twenty case studies, plus switching the language and writing an email. Typing matches without Polish diacritics ("uslugi" finds "Usługi"), every word must appear, labels that start with the query rank first. Built from the same data as the menus (`src/lib/palette.ts`, unit-tested; the About list now lives once in `src/data/siteMap.ts` for both); reuses the installed cmdk + Radix dialog, styled with the site's tokens. A jump to a case study opens it: `/case-studies` now also reacts to router hash changes, which fire no `hashchange`; a malformed `%` escape in such a link no longer throws inside the page (`src/lib/caseStudyHash.ts`, tested). The shortcut is the platform's own (Ctrl+K, or only ⌘K on a Mac, where Ctrl+K in a text field deletes to the end of the line), decided by the tested `isPaletteShortcut`; the button is a 44px tap target on phones. The diacritic folding is one function, `normalizeText` in `src/lib/text.ts`, shared with the service pages' tag matching.
- `/ksiazki`: the two free books on their own page (Kamil: "zrób stronę pod te książki, nie trzeba powtarzać całości"). The About menu's Books item now goes there instead of the bottom of the whole `/o-mnie` page. Same `beyond` copy from `i18n.ts`, two cards with the PDF downloads; Polish on the server like its `/o-mnie` sibling; in the sitemap.
- A before/after drawing in the hero of every service and area page (`src/components/illustrations.tsx`, twelve inline SVGs keyed by slug, each with a title and description in the page language; Kamil: "nie ściana tekstu, obrazki per usługa"). Drawn by a workflow and then reviewed by a visual critic pass: arrows re-aimed, faint strokes raised to ≥ 3:1, ids namespaced per slug so two drawings on one page cannot collide, no fonts or rasters, every colour a site token with a fallback. The dashed "flow" paths crawl only under `prefers-reduced-motion: no-preference`; the hero becomes two columns from 920px.
- `/case-studies` in Polish: all twenty studies translated (`src/data/caseStudies.pl.ts`, exported as `CASE_STUDIES[lang]`) and the page's labels with them, switched by the same language store as the rest of the site (Kamil: "realizacje się nie tłumaczą — miała być biblioteka tłumaczeniowa"). English stays the source of truth for facts; each translation was checked by an independent reviewer against the original for changed numbers, dates, names or claims, and a unit test keeps the two editions in lockstep (same slugs and order, same stacks, same decision count, every two-digit-plus number carried over). The server still renders English on this English-slug route; the reader's saved choice applies after hydration.
- **Obszary** (`/obszary`, `/obszary/$slug`): six area pages by business function — sales and marketing, customer service, administration and documents, HR and recruitment, data and reporting, unusual processes and AI — PL + EN, in the header as its own dropdown between Usługi and Realizacje (the same six letsautomate.pl uses; Kamil's call). Copy was drafted and then adversarially fact-checked per area against `caseStudies.ts`/`services.ts` (`src/data/areas.ts`); an area with no fitting production system shows no proofs section instead of a stretched one. Service and area pages now render through one `ServicePageBody`; the page CTA goes to `/kontakt`.
- (same PR, follow-up) `SolutionTabs`: dropped the "Running in production" proof snippet from each tab panel. Kamil: don't need examples here — the six problem cards above already link out, and each service page has its own proofs.
- (same PR, follow-up) Hero eyebrow keeps the English professional title "AI Automation & Implementation Engineer · Builder & Operator" on the Polish homepage too (Kamil: some brand/personal character stays even in PL); hero sub shortened in both languages; `/kontakt#/o-mnie` anchor jumps ("Ways to work together", "Books") now use the router's own `defaultHashScrollIntoView` instead of a hand-rolled effect that the router's `scrollRestoration` was overriding; `/blog` no longer invents topic titles, just says posts are coming.
- `/o-mnie`: the personal half of the old homepage (About, capabilities, ways to work together, contact, books) under its own heading and stats; the homepage keeps the offer, the Work list and contact. Both render from `src/components/HomePage.tsx` (`variant`). The header's About dropdown lists About me, Capabilities, Ways to work together, CV, AI system and Books; the hero CTA goes to `/kontakt`; `/o-mnie` renders Polish on the server.
- One header on every page (`src/components/SiteHeader.tsx`, mounted in `__root`): Services and About open as click dropdowns (Escape / click outside / navigation close them), Case studies, Blog, language switch and a "Free consultation" CTA; below 900px a full-screen sheet with the groups as accordions. Subpages lose their "← kamiljan.com" bars and duplicate language toggles.
- `/kontakt`: free 30-minute consultation page — how to prepare, what you leave with, and a form sent through the chat's proven lead path (`submitLead` → Resend) with a honeypot; `consultMessage()` unit-tested.
- `/blog`: in the menu before the first post; `noindex` and out of the sitemap until it has content.
- Homepage offer block after the hero, in the buyer's order (modelled on how letsautomate.pl leads): "Sound familiar?" — six problem cards, one per service, each linking to its /uslugi page — then "How I can help" — six switchable tabs (roving tabindex, arrow keys) showing the service, its lead and one system running in production. Both read from `src/data/services.ts` (`pickHomePains`, unit-tested), so the service pages stay the single source of the copy.
- Pipeline jakosci: CI (build/lint/typecheck/test/semgrep/audit/licencje), Claude review na PR, szablony dokumentacji
- `docs/ARCHITECTURE.md`, `docs/GLOSSARY.md`, `docs/adr/0001-*` and `0002-*`, filled-in `docs/RUNBOOK.md`, `docs/quality/BACKLOG.md`
- `LICENSE`, `.env.example`
- Vitest (`npm test`) + unit tests for the pure helpers in `src/server/lead.server.ts` (`esc`, `br`, `isValidEmail`)

### Changed

- Home page copy (Kamil: "systemem" said nothing concrete, and "none of these hurt at ten orders" is not true). The hero now says what replaces the process, with examples backed by case studies: a quote calculator instead of a spreadsheet (field-pricing-pwa), data that moves between programs on its own (Integrations), an AI assistant that answers customers (voice agent). It also names the cost to the company. The pains intro on the home page and on the Internal systems service page no longer claims a volume threshold: a process can hurt from the first order. The home page also drops "Each card opens the page on how it gets fixed" and, in "How I can help", "Each opens a page with the problems, the approach and systems already running in production" (Kamil: the cards say that themselves).
- Site-wide copy pass (Kamil, reviewing the live site): no sentence dashes anywhere in visible text. About 940 em and spaced en dashes in pages, data, the chat's canned messages and emails became full stops, commas or colons, done by five agents on separate files and checked by a script (no digit changed, only code comments keep a dash); page titles use "|". The chat bot's format rules now forbid dashes too. Also from Kamil's notes: the Internal systems lead is rewritten in plain words with one real example (the 13-stage B2B order platform), "including when that means a smaller job" and the About paragraph "AI agents write the code..." are gone, the contact line no longer says "every relevant enquiry", the index pages of services and areas drop their "each page..." lines, and the short book says "about an hour" (the tilde read as a minus sign). "Selected projects" on the home page link to the public GitHub write-up repos instead of the live sites (MAS Group, Flyt, QuickFix, Reykjawwwik, Ekomoc CRM; Sleipnir has no repo, so no link; a row that opens GitHub says "GitHub", and Workshop 3.0 keeps its live-app link because its repo is private), with the build years corrected to 2026 for Flyt, QuickFix, Reykjawwwik and Ekomoc.
- Command palette: every opening starts with an empty search. Ctrl+K pressed while it was still animating closed reused the mounted dialog with the last query, which made the required E2E check fail on CI; the palette test now picks case studies by keyboard and presses Ctrl+K again during the closing animation, where it fails without the fix (the same fix as in #42, carried here so this PR's required check is green).
- `/claude` rewritten for a human reader (Kamil: "pomyśl o osobie, która by to czytała … a nie jakby maszyna do maszyny gadała"). A fresh-context reviewer read the page as a head of operations and as a CTO and flagged undefined jargon, noun lists without verbs, aphorisms instead of explanations and calqued Polish; 237 field-level edits across both languages now say the plain thing first with the technical term in brackets once (gate, hook, scar, tier, k-of-n, CI, MCP), the intro adds what a client gets, diagram labels read as Polish (sejf, pośrednik, adwokat diabła, "każda bramka ma test"). Every fact and number is unchanged — the rewrite script refuses any field whose digits differ, with two flagged exceptions where "0-token" became words ("no AI tokens", "SCRIPTS"); fields that contradict each other on the page (hard stops, tools, routines) are left for Kamil to decide.
- Hero drawings split into one file per drawing (`src/components/illustrations/`, `Frame.tsx` + `index.tsx` with the single `Illustration` export) after the department review flagged the 1898-line module; a unit test now renders every service and area slug in both languages, so a typo in the slug map fails CI instead of silently leaving a hero without its drawing. The two-column hero applies only when a drawing is present (`:has`).
- Service and area pages (`/uslugi/$slug`, `/obszary/$slug`) redesigned so they read like a landing page, not a wall of text (Kamil: "kiepsko się czyta, więcej bajerów"): hero with a soft glow, the three micro-promises as chips and two CTAs; problem cards with a line icon per tag; the answer as a numbered timeline; proofs as metric tiles; a new four-step "How we work together" rail (free 30-min call → process map and quote → built in stages → training and handover); FAQ as an accordion whose questions are real `h3`s; a closing CTA card; a reading-progress bar. Sections fade in on scroll only when JS is running and only for elements below the fold — under `prefers-reduced-motion` or without JS the page is the plain document. Landmarks fixed on the way: one `<main>` per page (there was none), hero is a `section` so the site bar stays the only banner. Old `.svc-*` body rules removed; the index pages keep theirs. Contrast measured ≥ 8.5:1 on every text run.
- `/case-studies`: one list instead of two. The dark TOC card grid above the white study list is gone; each white card now shows the number, title, a two-line preview and the first three tags before it is opened — the information the TOC cards carried — so nothing is listed twice and the white headers are no longer bare titles. Dead `.cs-toc*` CSS removed.
- CetusPro line (hero EN+PL, `/kontakt`): "close to 40 developers" / "blisko 40 developerów".
- Removed the `supplier-feed-to-storefront` case study (NextCar shop): no AI in it, and the portfolio sells AI delivery. NextCar stays listed as a live system; the "20 case studies" line in the root FAQ schema now matches the list.
- `useLang` is one shared store (`useSyncExternalStore`) instead of a `useState` copy per route, so the header's language switch changes the page under it; hydration renders the server language first. `resolveLang` and `ssrLangFor` are pure and unit-tested. The homepage, CV and AI-system pages drop their own copies.
- Hero (EN + PL) sells the outcome for the company, not the person: "Your business. Less manual work / chaos / retyping / overhead / guesswork", a sub that names the processes people search for (invoices, orders, quotes, the spreadsheet), CTA "Show me your process", and a line that bigger builds run with CetusPro, a Rzeszów software house. PL eyebrow carries the searched phrases "automatyzacja procesów" and "wdrożenia AI w firmie".
- Hero sub (EN + PL): "fine-tuned AI coding agents" / "dostrojonymi agentami AI" — the agents run under Kamil's own hooks, review gates and skills, not stock.
- `/claude`: the "Self-evolution cycle" routine (opt-in, off) is no longer listed — no point describing something that is switched off.
- About (EN + PL) shortened to two paragraphs per Kamil: the paying-side paragraph stays; the second is now one sentence (AI agents write the code, Kamil reviews it and owns spec/architecture/review/deploy/the phone when it breaks); the proof list and the third paragraph are gone. Trade-off: the sticky photo holds ~100px again instead of ~420px.
- README: corrected the "no backend" claim (the AI chat bot and lead capture run as real server functions calling Anthropic + Resend), documented env vars and testing
- `src/server/lead.server.ts`: magic numbers (transcript/message/name/email length caps) named as constants; `isValidEmail` extracted from an inline regex check
- `src/components/Hero3D.tsx`: `renderer` typed as `THREE.WebGLRenderer | null` instead of `any` (the only hand-written `any` in the repo; `src/routeTree.gen.ts`'s are router-plugin-generated and out of scope), with the two resulting "possibly null" call sites (`tick`'s render loop, `onResize`) guarded instead of suppressed
- About (EN + PL) rewritten around what a client actually pays for now that code is cheap: 12+ years on the paying side of software (knows where revenue and margin are, says what not to build), AI agents write the code while Kamil owns spec/architecture/review/deploy/production, and problems get named early (every case study lists rejected alternatives and limitations). Third paragraph added, so the sticky photo now holds ~3× longer.
- Homepage copy (EN + PL) no longer says "I write the code" / "Full-stack": hero sub, the 2024 milestone and the Websites & Products card now match the CV and the chat bot's rule — AI coding agents write the code, Kamil owns the spec, architecture, review and deploy.

### Fixed

- Mobile pass (Kamil: "sprawdź każdą stronę, podstronę, sekcję i element pod mobile"). Every route was measured at 360/390/430 px with the mobile-optimization skill's checks, then reviewed section by section from screenshots. Fixed in `src/site.css`: the sibling links on service and area pages (`.sl-more-link`), the contact row (WhatsApp, e-mail, CV) and the chat's starter questions are now 44 px tap targets; labels and tags that were 10-11.5 px are 12 px on phones; case-study text is 16 px; the solution tabs show that they scroll sideways (edge fade, snap); the /claude diagrams say "Swipe" / "Przesuń"; /kontakt no longer leaves an 80 px gap above the form; and on /o-mnie the portrait no longer slides under the contact heading (a desktop `.contact:has(+ .beyond)` rule outranked the phone padding: 257 px overlap, now 0). The rules sit in the existing media blocks of each component; only a 12 px type floor stays last in the file, because some labels get smaller sizes in later component rules. CI now also runs `e2e/mobile.spec.ts` on a Pixel 7 emulation (new "mobile" Playwright project): no sideways scroll on seven key pages, the /o-mnie portrait below the text, 44 px sibling links, 16 px form fields. The command palette now starts every opening with an empty search: Ctrl+K pressed while it was still animating closed reused the mounted dialog with the last query (found by the E2E palette test on CI, which now picks case studies by keyboard and presses Ctrl+K again during the closing animation, where it fails on every one of 20 runs against a palette with no reset, and passes on all 20 with it). The reset happens while the opening renders rather than in an effect, so the old query is not painted for a frame first; that part is a rendering detail no browser test can observe.
- The required mutation-testing check reported every mutant as survived (PR #38: 0 of 104 killed), because Stryker's Vitest runner 10.0.0 runs no tests at all on Vitest 5 (stryker-js#6210); the job now runs Stryker on Vitest 4.1.11 only (the project stays on Vitest 5) and fails loudly if a covered mutant "survives" without a single test having run. The same PR #38 files now score 100 % (`caseStudyHash.ts`, 24/24) and 90.8 % (`palette.ts`, 69 killed, 6 survived); how to reproduce it locally is in `docs/RUNBOOK.md`.
- The required "E2E smoke (Playwright)" check now runs tests. It only ran when a Playwright config existed, and the repo had none, so it always passed empty. `playwright.config.ts` builds the site as a Node server (`vite preview` can't serve this build) and runs `e2e/smoke.spec.ts` plus a new `e2e/palette.spec.ts`: the palette opens from the button and Ctrl+K, search finds pages without diacritics, and two jumps on `/case-studies` each open the study they point at. `@playwright/test` 1.63.0 added as a dev dependency. How to run and debug: `docs/RUNBOOK.md`.
- Claims the case studies do not back, taken off the site before it becomes the source for an email agent (letsautomate.pl comparison, 2026-09-15): Sleipnir "top-rated / najwyżej ocenianych" → "well-reviewed / dobrze oceniany" as the case study says (home, CV, chat bot); "voice agents, WhatsApp bots … running in production" → what runs in production (automated status calls and SMS), with the two-way voice agent named as still being hardened; "AI champions" dropped; the chat bot no longer says the code is "hand-built" or that answers are "grounded with RAG", and its "add AI to my business" quick reply lists only what runs (n8n, LLM integrations, automated status calls and SMS); the `/case-studies` intro says twenty projects instead of "a few". The global FAQPage JSON-LD is gone from every page (its six questions were visible on none of them and named nextcar.is and RetellAI "in production"); service and area pages now publish their own visible FAQ as FAQPage. `src/data/claims.test.ts` fails CI if any retracted phrase comes back anywhere in `src/`, including a phrase split across a string concatenation.
- Quick wins from the overnight audit (verified live, no new claims): faint text on the `/o-mnie` role cards and in the footer raised from 2.1:1 to the secondary text colour; `/cv` dates/locations and `/case-studies` number/time tones darkened past AA on the paper background; `/cv` contact links are 44px tap targets on phones; `/kontakt` shows the form before the preparation notes on phones (it sat 1 340px down a 2 165px page); `/case-studies#slug` deep links stop below the fixed menu instead of under it; the `/uslugi` closing CTA goes to `/kontakt` like every other page (it was a bare mailto), and the `/obszary` heading no longer repeats the button label. Two stale comments that still said the service pages were kept out of the header are rewritten.
- `/claude` diagrams: labels no longer spill out of their boxes or run into each other (Kamil's screenshot of "Życie jednej zmiany"). Chip, branch and box labels now wrap through `src/lib/wrapLabel.ts` (unit-tested: breaks before a "+" when both halves fit, otherwise on spaces), and the boxes, rows and SVG height follow the longest wrapped label, so later copy changes cannot overflow either. Pipeline chips are wider, sit in two rows with a gap, and clear the page edge and the LIVE box. Measured in the browser on all six diagrams in PL and EN: 0 labels outside their box, 0 text outside the SVG, 0 overlapping labels.
- `<html lang>` follows the page language (was a fixed `en` even over Polish copy on `/uslugi`, `/obszary`, `/kontakt`, `/blog`, `/o-mnie`): Polish on the server for those routes, English elsewhere, the reader's choice after hydration.
- Custom cursor no longer stutters while selecting text: it was positioned with `left`/`top` on every mousemove (a layout write per event, plus a drop-shadow filter repaint); now `transform: translate3d` coalesced to one write per animation frame, `will-change: transform`.
- Capabilities deck now works on phones too: it was desktop-only (≥769px) with an accordion below. The sticky stack is the base rule at every width (`--deck-top` = nav + 12px: 132px desktop / 76px phone, `--deck-step` 12px / 10px); the accordion (button, chevron, `openCap` state) is removed since neither breakpoint used it any more, and `.cap-title` is a real `h3`.
- CI `mutation.yml`: the `labeled`/`unlabeled` trigger from #15 now re-runs the job only when the label involved is `allow-low-mutation` (job-level `if` checks `github.event.label.name`); any other label no longer starts a full Stryker run. Found by the PR review, merged past it by mistake.
- CI `mutation.yml` also runs on `labeled`/`unlabeled`, so applying `allow-low-mutation` (the workflow's own escape hatch) re-evaluates the check instead of leaving a red one until the next push.
- CI `mutation.yml`: Stryker ran from the `npx -p` cache and could not resolve `@stryker-mutator/vitest-runner` nor `typescript` (both resolve from the project), so the first PR that actually exercised the gate (#13) died with `ERR_MODULE_NOT_FOUND`. Now installed into the project (`--no-save`) after `npm ci`.
- About (desktop): the photo's `position: sticky` engages again — `.about` had `overflow: hidden`, which made it a scroll container, so the photo scrolled 1:1 with the page; switched to `overflow: clip`. It holds for as long as the text column is taller than the photo (~108px at 1280px wide).
- Capabilities (desktop ≥769px): the four cards are now a stack of sheets — each one pins under the 120px fixed nav (`position: sticky`, tops 132/144/156/168px) and the next slides over it. Phones keep the accordion.
- `position: sticky` now works site-wide: `overflow-x: hidden` on both `html` and `body` made `body` its own scroll container, so nothing could stick; switched to `overflow-x: clip` (with `hidden` kept as fallback). `.cap` uses `overflow: clip` for the same reason.
- Security: `npm audit fix` (lockfile only, no major bumps) — clears the 5 high advisories (`sharp` libheif GHSA-rgj7-g3m4-5g8c via `miniflare`/`wrangler`/`@cloudflare/vite-plugin`, and `js-yaml`) that turned the CI audit step red. 2 moderate left (`vitest` → needs major 5.x).

### Security

- Lead path (chat + `/kontakt`) is rate-limited: 5 leads per client IP in 10 minutes, then refused with a message; past 40 an hour per instance the email still goes out without the Anthropic brief, so a script loop cannot run up the model bill. Every failure in `lead.server.ts` now logs a `[lead] …` line (no personal data) instead of returning silently; RUNBOOK and README describe the real lead path.
- `vitest` 3.2 → 5.0 (dev only) clears the last 2 moderate advisories (`vitest`, `@vitest/mocker`); `npm audit` now reports 0. vitest 5 needs Node ≥22.12, so CI (`quality.yml`, `mutation.yml`) moves from Node 20 (EOL) to 24.
